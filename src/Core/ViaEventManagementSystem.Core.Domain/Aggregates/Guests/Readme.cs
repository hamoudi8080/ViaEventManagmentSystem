namespace ViaEventManagementSystem.Core.Domain.Aggregates.Guests;

public class Readme
{   //Create a “Common” directory. Here we put stuff which may be shared across the Domain project,
    //and may not otherwise have an obvious location.
    //We need base classes for AggregateRoot, Entities, and ValueObject, and maybe others.
    
    
    
    
    
    
   //Immutability: Value objects should be immutable, meaning their state cannot be modified after creation.
   //This ensures consistency and avoids unexpected changes.
   //In the provided implementations, the properties are read-only, which is good practice for immutability.
}