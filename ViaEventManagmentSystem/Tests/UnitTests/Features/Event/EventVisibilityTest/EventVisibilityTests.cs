namespace UnitTests.Features.Event.EventVisibilityTest;

public class EventVisibilityTests
{
    /*
    [Fact]
    public void PrivateVisibility_ShouldHaveValueZero()
    {
        // Arrange
        var visibility = EventVisibility.Private;

        // Assert
        Assert.Equal(0, visibility.Value);
    }

    [Fact]
    public void PrivateVisibility_ShouldHaveDisplayNamePrivate()
    {
        // Arrange
        var visibility = EventVisibility.Private;

        // Assert
        Assert.Equal("Private", visibility.ToString() );
    }

    [Fact]
    public void PublicVisibility_ShouldHaveValueOne()
    {
        // Arrange
        var visibility = EventVisibility.Public;

        // Assert
        Assert.Equal(1, visibility.Value);
    }

    [Fact]
    public void PublicVisibility_ShouldHaveDisplayNamePublic()
    {
        // Arrange
        var visibility = EventVisibility.Public;

        // Assert
        Assert.Equal("Public", visibility.DisplayName);
    }

    [Fact]
    public void PrivateAndPublicVisibilities_ShouldNotBeEqual()
    {
        // Arrange
        var privateVisibility = EventVisibility.Private;
        var publicVisibility = EventVisibility.Public;

        // Assert
        Assert.NotEqual(privateVisibility, publicVisibility);
    }
    */
}